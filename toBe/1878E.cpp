#include<bits/stdc++.h>
using namespace std;

void fxn(){
    int n;
    cin >> n;
    vector<int> a(n, 0);

    for(int& i : a){
        cin >> i;
    }

    // SegTree tree(monotonicMax);

    // int q;
    // cin >> q;

    // for(int i = 0; i < q; i++){
    //     int l, r;
    //     cin >> l >> r;
    //     cout << tree.rangeMax(l, r) << endl;
    // }
}

int main(){
    int t;
    cin >> t;
    while(t--){
        fxn();
    }
}